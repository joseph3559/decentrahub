# Server Configuration
PORT=5001
NODE_ENV=development

# API Keys & Secrets
ALCHEMY_API_KEY_LENS_CHAIN=YOUR_ALCHEMY_API_KEY_FOR_LENS_CHAIN
PINATA_API_KEY=YOUR_PINATA_API_KEY # If using Pinata for IPFS
PINATA_SECRET_API_KEY=YOUR_PINATA_SECRET_KEY

# Lens Protocol Configuration (if specific SDK keys are needed beyond Alchemy)
# LENS_API_URL=https://api-v2.lens.dev # Or sandbox equivalent

# Bonsai Protocol Configuration (if specific keys are needed)
# BONSAI_API_KEY=YOUR_BONSAI_API_KEY

# Database (Example for MongoDB)
# MONGO_URI=mongodb://localhost:27017/decentrahub

# JWT Secret (If implementing JWT for any backend-specific auth)
# JWT_SECRET=YOUR_VERY_SECRET_JWT_KEY
